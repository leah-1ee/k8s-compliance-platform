apiVersion: v1
kind: Namespace
metadata:
  name: compliance-system
  labels:
    app.kubernetes.io/part-of: compliance-platform

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-classifier
  namespace: compliance-system
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: compliance-ai-classifier
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compliance-ai-classifier
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: compliance-ai-classifier
subjects:
  - kind: ServiceAccount
    name: ai-classifier
    namespace: compliance-system

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-classifier-data
  namespace: compliance-system
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-classifier
  namespace: compliance-system
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ai-classifier
  template:
    metadata:
      labels:
        app: ai-classifier
        app.kubernetes.io/part-of: compliance-platform
    spec:
      serviceAccountName: ai-classifier
      containers:
        - name: ai-classifier
          image: docker.io/leeon3345/compliance-ai-server:0.2.28
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: RESPONSE_SERVER_URL
              value: "http://response-server.compliance-system.svc.cluster.local:5000"
            - name: CLUSTER_NAME
              value: "school-cloud"
            - name: PUBLIC_BASE_URL
              value: "https://compliance-ai-console.shares.zrok.io"
            - name: GRAFANA_URL_INTERNAL
              value: "http://monitoring-grafana.monitoring.svc.cluster.local:80"
            - name: GRAFANA_CLIENT_URL_INTERNAL
              value: "http://monitoring-grafana.monitoring.svc.cluster.local:80"
            - name: GRAFANA_ADMIN_URL_INTERNAL
              value: "http://kubeowl-admin-grafana.monitoring.svc.cluster.local:80"
            - name: GRAFANA_MASTER_DASHBOARD_UIDS
              value: "compliance-overview,compliance-runtime-detection"
            - name: PROMETHEUS_URL
              value: "http://monitoring-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090"
            - name: AI_SERVER_PROXY_BASE_URL
              value: "http://ai-classifier.compliance-system.svc.cluster.local:8000"
            - name: GRAFANA_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_ADMIN_USER
                  optional: true
            - name: GRAFANA_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_ADMIN_PASSWORD
                  optional: true
            - name: GRAFANA_CLIENT_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_CLIENT_ADMIN_USER
                  optional: true
            - name: GRAFANA_CLIENT_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_CLIENT_ADMIN_PASSWORD
                  optional: true
            - name: GRAFANA_ADMIN_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_ADMIN_ADMIN_USER
                  optional: true
            - name: GRAFANA_ADMIN_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-credentials
                  key: GRAFANA_ADMIN_ADMIN_PASSWORD
                  optional: true
            - name: SQLITE_PATH
              value: "/data/compliance-ai-server.sqlite3"
            - name: DEMO_CLUSTER_NAMES
              value: "school-cloud,boanlab-cloud"
            - name: DEV_AUTH_ENABLED
              value: "false"
            - name: DEV_AUTH_EMAIL
              value: "demo@school.test"
            - name: SESSION_COOKIE_SECURE
              value: "true"
            - name: ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ai-classifier-admin
                  key: ADMIN_TOKEN
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: ai-google-oauth
                  key: GOOGLE_CLIENT_ID
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ai-google-oauth
                  key: GOOGLE_CLIENT_SECRET
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: ai-classifier-data
              mountPath: /data
      volumes:
        - name: ai-classifier-data
          persistentVolumeClaim:
            claimName: ai-classifier-data

---
apiVersion: v1
kind: Service
metadata:
  name: ai-classifier
  namespace: compliance-system
  labels:
    app: ai-classifier
    app.kubernetes.io/part-of: compliance-platform
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app: ai-classifier
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
